# repovec-secret-store

Nursery interfaces for composing a secret-protection codec with a durable
repository.

This package does not prescribe `systemd-creds`, a filesystem library, or a
Podman-secret implementation. It is not published and makes no compatibility
promise. See `docs/nursery-crates.md` and ADR 005.
