# repovec-systemd-probe

Nursery evidence types and ports for checking Quadlets with Podman's generator
and native units with `systemd-analyze verify`.

This package does not parse units or control the live systemd manager. It is not
published and makes no compatibility promise. See `docs/nursery-crates.md` and
ADR 002.
